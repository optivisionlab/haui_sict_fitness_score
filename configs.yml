version: 1.0

RESNET_EMBEDDING_PATH: "vggface2"
QDRANT_URL: !ENV ${QDRANT_URL}
MONGDB_URI : !ENV ${MONGDB_URI} 
MONGDB_DB : !ENV ${MONGDB_DB}

DEVICE: &dev !ENV ${DEVICE}

VECTOR_SIZE: 512

MTCNN_CONFIG:
  image_size: 512
  device: *dev